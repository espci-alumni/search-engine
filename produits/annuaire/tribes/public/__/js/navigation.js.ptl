var fiche = location.pathname.replace(/.*\//, '');

var navigationOutputFilter = window.navigationOutputFilter || function($a, $lastSearch, $fichePrev, $ficheNext, $fichePos, $ficheCount)
{
    return $a.replace($lastSearch, '/annuaire/' + $lastSearch);
}

<!-- INLINE js/navigation.js -->
